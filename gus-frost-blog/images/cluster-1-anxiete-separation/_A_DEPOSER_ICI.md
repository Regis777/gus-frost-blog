# Déposer ici les 5 images du pilier C1

Enregistre chaque visuel **avec EXACTEMENT ce nom de fichier** dans ce dossier.
Le script d'intégration fait la correspondance automatiquement (nom = ce qui suit `REMPLACER_` dans le HTML).

| Fichier à déposer | Image | Taille cible |
|---|---|---|
| `hero-anxiete-separation.jpg` | Hero / en-tête (chien près de la porte) | 1600 × 900 |
| `griffures-porte.jpg` | Griffures sur le bas d'une porte | 1080 × 1080 |
| `infographie-10-signes.jpg` | Infographie « 10 signes » | 1080 × 1350 |
| `coin-refuge-chien.jpg` | Coin refuge cosy | 1080 × 1080 |
| `tapis-lechage-occupation.jpg` | Chien sur tapis de léchage au départ | 1080 × 1080 |

- Formats acceptés : `.jpg` / `.jpeg` / `.png` / `.webp`. Si tu changes l'extension, garde le même nom de base (ex. `hero-anxiete-separation.png`).
- Poids conseillé : < 400 Ko par image (le CDN Shopify les sert ensuite). Pas de texte sur les photos ; l'infographie est la seule à contenir du texte → vérifie l'orthographe.
- Quand les 5 sont là, je les uploade sur les fichiers Shopify, je remplace les `src` et je republie le brouillon.
